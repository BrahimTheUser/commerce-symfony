<form method="post" action="{{ path('app_sub_category_delete', {'id': sub_category.id}) }}"  class="btn btn-danger btn-sm mt-4" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sub_category.id) }}">
    <button class="btn">Delete</button>
</form>
